# Caminho customizado do Whisper (opcional — auto-detecta se omitido)
# WHISPER_PATH=/usr/local/bin/whisper

# CORS origin para produção (opcional — padrão: localhost)
# CORS_ORIGIN=https://meuapp.com

# Porta do servidor (opcional — padrão: 3001)
# PORT=3001
